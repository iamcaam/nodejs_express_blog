<% layout('layout') -%>
<h1><%= title %></h1>

<ul>
    <% book_list.forEach(function(book){%>
        <li>

                <a href="<%= book.url %>"><%= book.title %></a><p>(<%= book.author.name %>)</p>
            
        </li>
      <%}); %>


</ul>